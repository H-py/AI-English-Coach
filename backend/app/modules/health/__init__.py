"""Health check module.

A minimal vertical slice that demonstrates the standard module layout:
``router.py`` for endpoints and ``schemas.py`` for Pydantic models. New
feature modules should follow this same structure.
"""
