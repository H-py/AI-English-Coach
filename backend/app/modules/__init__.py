"""Feature modules package.

Each sub-package is a self-contained vertical slice (router, schemas,
services, models) so that new features can be added without touching shared
infrastructure. See ``app/modules/health`` for the canonical template.
"""
